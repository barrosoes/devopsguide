apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: boutique-frontend
spec:
  ingressClassName: nginx
  rules:
  - host: boutique.platformengineering.com.br
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 80

