import { motion } from "framer-motion";

export default function App() {
  return (
    <div className="min-h-screen bg-slate-950 text-white overflow-x-hidden">

      {/* HERO */}
      <section className="max-w-6xl mx-auto px-6 pt-32 pb-24 text-center">
        <motion.h1
          initial={{ opacity: 0, y: 40 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8 }}
          className="text-6xl font-bold bg-gradient-to-r from-blue-500 to-cyan-400 bg-clip-text text-transparent mb-6"
        >
          Anderson Barroso
        </motion.h1>

        <motion.p
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ delay: 0.3 }}
          className="text-xl text-slate-400 mb-10"
        >
          DevOps Engineer • Oracle DBA • Kubernetes • GitOps • MultiCloud
        </motion.p>

        <div className="flex justify-center gap-6">
          <a
            href="https://github.com/SEU_USUARIO"
            className="bg-blue-600 hover:bg-blue-700 px-8 py-3 rounded-2xl shadow-xl transition"
          >
            GitHub
          </a>

          <a
            href="https://linkedin.com/in/SEU_LINK"
            className="bg-slate-800 hover:bg-slate-700 px-8 py-3 rounded-2xl shadow-xl transition"
          >
            LinkedIn
          </a>
        </div>
      </section>

      {/* EXPERTISE */}
      <section className="max-w-6xl mx-auto px-6 py-24 border-t border-slate-800">
        <h2 className="text-3xl font-semibold text-center mb-16">
          Core Expertise
        </h2>

        <div className="grid md:grid-cols-3 gap-8">
          {[
            "Kubernetes (OKE) & ArgoCD",
            "CI/CD & Automation",
            "MultiCloud Architecture",
            "Performance & Observability",
            "Infrastructure as Code"
          ].map((item, index) => (
            <motion.div
              key={index}
              whileHover={{ scale: 1.05 }}
              className="bg-slate-900 p-6 rounded-2xl shadow-xl text-center"
            >
              {item}
            </motion.div>
          ))}
        </div>
      </section>

      {/* DATABASE EXPERTISE */}
      <section className="max-w-6xl mx-auto px-6 py-24 border-t border-slate-800">
  	<h2 className="text-3xl font-semibold text-center mb-16">
    	  Database Expertise
  	</h2>

  <div className="grid md:grid-cols-2 gap-8">
    {[
      "Oracle RAC (High Availability Clusters)",
      "Oracle Data Guard (Disaster Recovery)",
      "Performance Tuning & Optimization",
      "Enterprise Database Architecture"
    ].map((item, index) => (
      <motion.div
        key={index}
        whileHover={{ scale: 1.03 }}
        className="bg-slate-900/60 border border-slate-800 p-6 rounded-2xl shadow-lg text-center"
      >
        {item}
      </motion.div>
    ))}
  </div>
</section>
	
      {/* FOOTER */}
      <footer className="text-center text-slate-600 text-sm py-12 border-t border-slate-800">
        © {new Date().getFullYear()} Anderson Barroso • Deployed on OKE via GitOps
      </footer>

    </div>
  );
}

